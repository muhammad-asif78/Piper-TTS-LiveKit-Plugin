# Piper-TTS-LiveKit-Plugin
Low-latency, local Text-to-Speech integration for LiveKit Agents using Piper ONNX voices. Supports streaming playback, configurable voice/model paths, and realtime latency logging (STT, LLM, TTS) for voice assistant benchmarking.
